## 2026-06-29

- 将 `test/pe_sim/run_comp.sh` 转换为 Windows `.bat` 版本 `test/pe_sim/run_comp.bat`
- 深入讲解了 `pe_top.v` 完整架构：Generator(Gen1/Gen2双通道)、Executor、MAC仲裁器、16-Bank B存储、旋转Mux、Ping-Pong累加器、Product FIFO路由、C Bank写入
- 基于 `pe_cluster.v` 创建了完整加速器 `accelerator_top.v`，新增6个模块：
  - `a_global_buffer.v` — 全局A矩阵CSR存储
  - `b_global_buffer.v` — 全局B矩阵CSR存储
  - `c_global_buffer.v` — 全局C矩阵dense输出存储（16 bank）
  - `pe_load_ctrl.v` — A轮询分区+ B广播加载控制器
  - `pe_drain_ctrl.v` — PE C bank回读 + C全局写入控制器
  - `accelerator_top.v` — 顶层5状态FSM集成（S_IDLE→S_LOAD_A→S_LOAD_B→S_COMPUTE→S_DRAIN→S_DONE）
- 参考了 `scala/` 目录下的Chisel3工程（GCN加速器），借鉴了Core.scala的三阶段FSM、Compute.scala的sDataMove分发模式
