Kintex UltraScale 系列的xcku040-ffva1156-2-e开发板

读延迟:C bank 读端口是 1 拍

同步读,host 侧已按"先给地址、下一拍取数"处理——上板 host 驱动同样要遵守这个时序。

和我预期的不太一样。做加减法应该直接跳过乘法器，每次先把A中同一行的16个非零数发到accum_bank_16,接着把B中统一行16个非零元素发到accum_bank_16,如果有col_id相同的，直接相加/减，之后直接输出到C buffer;然后再发送A中同一行非零元素，依旧是16个为一组，遇到col_id相等的就进行相加/减，并输出，就这样交替进行，直到